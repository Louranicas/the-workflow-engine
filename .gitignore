# Rust (post-G9 unlock)
/target/
**/*.rs.bk
*.pdb
# Cargo.lock: COMMITTED (workflow-trace ships binaries — reproducible builds
# require lockfile; per Rust convention bin crates commit Cargo.lock, lib
# crates don't). Was excluded pre-G9; uncomment to revert if workflow-trace
# becomes a pure library (it won't — see Genesis Prompt v1.3 § 1).

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Bacon
.bacon-locations

# Logs
*.log
logs/

# Local env
.env
.env.local

# Claude Code local
.claude/scheduled_tasks.lock
.claude/status.json
.claude/settings.local.json
.claude/worktrees/

# Scaffold-mastery artefacts
/scaffold-gen.log

# SQLite scratch
*.db-wal
*.db-shm

# DO NOT IGNORE (explicit allowlist for spec-bearing dirs)
!ai_specs/
!ai_docs/
!ultramap/
!layers/
!modules/
!the-workflow-engine-vault/

# cargo-mutants output (ephemeral mutation-testing artifacts)
mutants.out/
mutants.out.old/

# cargo-mutants per-task target dir (separate from the default cargo target/)
target-mutants/

# Watcher ☤ deployment-watch journals (v0.1.1 hygiene resolution).
# These are live-appended by the synthex-v2 Watcher daemon every session
# and were causing "asymptotic dirty tree" (Zen Phase-1 finding). The
# historical content through v0.1.0 ship is preserved in commits up to
# b18ef0c ("docs(watcher): post-M0 journal snapshot — tick·1866"); further
# Watcher writes stay on-disk but out of git. Operator can `git add -f`
# to capture a snapshot if desired.
pre-framework-consolidation/WATCHER_DEPLOYMENT_WATCH_JOURNAL_S1001982.md
the-workflow-engine-vault/Watcher Deployment Watch Journal S1001982.md
