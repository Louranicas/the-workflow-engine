//! `wf-dispatch` — secondary workflow-trace binary owning modules m30-m33
//! (Bank → Select → Dispatch → Verify; Cluster G).
//!
//! Per Genesis Prompt v1.3 § 1. Stub at Cluster D Day-1 per the same
//! rationale documented in `wf_crystallise.rs`.

#![forbid(unsafe_code)]
#![warn(missing_docs)]

fn main() {
    println!("wf-dispatch · workflow-trace v0.1.0 (Cluster D Day-1 scaffold)");
}
